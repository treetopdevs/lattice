# Treehouse: unified delivery plan

Status: **PROPOSED, not adopted or implemented**. Prepared 2026-09-06 by comparing the two working-file roadmaps identified in the [comparison](treehouse-plan-comparison-2026-09-06.md). Repository baseline: `af84459bfc066b4ed405b99a02046b4f2c6315ee`, the locally available `origin/main`, not a fresh remote-status assertion. Worktree: `/Users/nicholas/develop/lattice-treehouse-unified-20260906`; branch: `codex/treehouse-unified-20260906`.

This is the proposed single execution ledger after adoption. R-numbers preserve the delivery roadmap's packet identity; new sub-IDs split decisions, builds and evidence. Plan 180 remains source material, not a second schedule to execute. Source plans retain their detailed contracts and STOP conditions until the named amendment explicitly changes them. No plan numbers 181–183 are allocated by this proposal.

## 1. Deliver one useful group, with demonstrated loss recovery

Build an empty Android group app for one assisted, text-only community of 9–15 people, targeting 12. Members create/join, post and edit offline, converge after reconnection, manage roles and removals, archive busy Threads and read retained history. Before the community pilot, demonstrate relay replacement, founder continuation, member re-admission and combined founder-plus-relay loss through packaged workflows and the exact physical candidate. Then enable Toolshed inside that group and run its separate household pilot. Township follows the group/module evidence.

Preserve a useful early result: a root-only, offline native preview can ship internally before founder-continuation implementation or hosting. It issues no member grants and makes no multi-device or recovery claim. A blocked authority or native-witness design stops the strong pilot; it does not erase the preview or authorize a weaker public release.

The operator, host OS administrators, backups and admitted transport peers can read their plaintext copies; the host can withhold service. The operator also provisions every new route, including rollover successors. Members supply semantic signatures; a relay operator cannot impersonate them. Removal cannot erase plaintext already held by a departed member.

### Evidence milestones

| Gate | Observable result | Completion rule |
| --- | --- | --- |
| G0 Contract | Core amendments adopted, risks tested, native approach decided | R01a, R02, R17a; operational/module approvals remain separately visible |
| G1 Preview | Empty packaged app retains a local Space/Thread and offline edits through restart | R10, R12; explicitly root-only and offline |
| G2 Working group | Enrolled apps converge; roles, removal, witness readiness, renewal and rollover are usable | R13, R14, R15; packaged evidence only |
| G3 Loss readiness | AF-1/AF-2/AF-3 and combined loss work; supported backup restore is separately proven | R16, R17c, R18, R19b, R20, R21b |
| G4 Android candidate | Exact signed artifact passes unrelated-phone and independent-witness gates | R22b; all G3 evidence reproduced or linked to the exact candidate |
| G5 Community pilot | At least 14 days with 9–15 participants, named support, loss rehearsals and go/no-go report | R23; elapsed time alone is insufficient |
| G6 Toolshed | Offline co-signed custody, factual ledger and subject-present receipts work in the group | R30, including independent third-device audit and 7–14-day household pilot |
| Later | Optional desktop host, physical iOS, measured expansion and Township | Independently gated R31–R35; none blocks G5 |

Dates are observation windows, not engineering estimates. G0 and G1 are parallel outcomes where their packet dependencies allow it. A milestone cannot backfill a missing lower evidence tier.

## 2. Baseline and first decisions

At the pinned baseline, reuse the carrier runtime, product isolation/migrations, Android distribution substrate, causal command-policy context, Treehouse copy contract, AF-1 runtime tests, and legacy Toolshed primitives. Plan 175's decision and Plan 179 step 1 exist; Plan 179's witnessed-beacon build remains future work. There is no production Treehouse domain/realm/app, signed catalog workflow, deployed WSS proof or AF-3 implementation established by these roadmaps. These are baseline facts, not a current hosted-status audit.

Recheck immutable source, PR/check evidence and actual files before each packet. Plan 172's prior work is reusable only with an identified SHA and equivalence proof; an unnamed worktree, stash or TODO row is never a prerequisite. The source checkout at `9601f146` is behind this baseline and must not silently supply implementation facts.

### Adoption register

Prepare reviewable amendments before collecting any required sign-off. Planning here supplies no operator signature, platform un-parking, deployment permission or claim of completed product behavior.

| Decision | Recommendation | Owner and gate |
| --- | --- | --- |
| Product order and archive vocabulary | Treehouse, then Toolshed module, then Township; exact Thread archive rules in section 4 | R01a, before R10 |
| Domain/catalog and catalog/deployment ordering | Permit fixture-backed root-only domain before catalog; local catalog code before public WSS | R01a explicitly amends Plan 158's two edges |
| Native/physical scope | Adopt Android camera/pairing/physical scope and a supported witness ceremony; LAN remains parked | R01b, before R13/R36/native ceremony; iOS remains R32a |
| Module isolation | Keep Treehouse app ID, database, key service and signing lineage; Toolshed becomes namespaces/replica permissions; legacy Toolshed IDs remain reserved | R01c actual Plan 158 countersign, before R26; does not block R10/R12 |
| Founder authority | Bounded successor, surviving renewal issuer, leased removable founder grants and pre-authorized catalog replacement | R02 probes and explicit contract, implemented in R04 |
| Witness establishment | Proposed 2-of-3 independent keys; separate beacon/succession/recovery purposes and per-replica pinning | R02/R17a; threshold is proposed until adopted |
| Epoch and renewal operations | Compare a daily reviewed signed-day cadence and seven-day lease window against measured fan-out; adopt exact units/window/bounds from probes | R02, before R14 profile enablement |
| Social continuity | Group-scoped old/new-key attestation plus new-key possession, followed by ordinary explicit admission | R19a; exact threshold/conflict/vocabulary decisions precede R19b |
| Expansion | Keep the observed pilot cohort first; inclusive 150 is only a proposed later target, requiring amendment of Plan 177's “under 150” | R35a, before R33; does not block core chat |

R01a adds current-order notes and narrow amendments rather than rewriting the entire Plan 158 history. Preserve protected README rows 121/122/178 (one 178 row), Plans 121/122 DONE sentences, Plan 158's pinned Plan 178 paragraph, and the build-map strings `plans 023-133` and `Phase G's audit surface is implemented by Plan 121`. Preserve Plan 178's required-sentence and prohibited-phrase structure. Only explicitly adopted vocabulary assertions change. Append a scoped note when a desired edit would touch an unrelated pin.

Mark Plan 152's additional LAN/CD1 program withdrawn, retain existing helpers, and disposition Plan 151's Treehouse work through R10–R15; any independent Township remainder belongs to R34. R01c/R26 own the Toolshed one-pager correction; if another earlier packet edits that copy, it also takes the full reader/host disclosure correction and its focused test.

## 3. One dependency ledger

All rows are **PLANNED**. `Core` means BEAM/TypeScript parity where semantics cross runtimes; `Packaged` means ordinary installed-app entry points; `Physical` means the exact signed artifact on real independent devices. `Adopted` requires the concrete amendment and any required sign-off. Sizes: S one bounded session, M several sessions, L sustained integration, XL multiple behavior slices in one integration unit. Risk and size are separate; estimates are calibrated against Plans 135–139 and 179 and revised after failing probes.

Rows are the authoritative dependency graph. Commas mean all prerequisites. “None” permits authorized entry work, not automatic approval of a proposed policy. Parent IDs denote all their stages; preparation and hardware collection can proceed before dependent enabling work. An implementation handoff adds the exact source plans and allowed files from section 9.

| ID | Packet / accountable function | Size | Risk | Requires | Exit evidence |
| --- | --- | --- | --- | --- | --- |
| R01a | Core contract / integrator | M | Medium | None | Adopted order, archive, two dependency amendments, protected pins green |
| R01b | Android and witness scope / operator + integrator | S | High | R01a, R17a | Adopted scope and deferred-ceremony amendment; actual sign-off |
| R01c | Toolshed module isolation / operator + integrator | S | Medium | R01a | Countersigned collision contract; reserved legacy identities |
| R02 | Founder lifecycle design / authority | M | High | R01a | Executable matrix: renewal, bounded successor, child roots and catalog recovery |
| R03 | Witnessed beacons / authority | L | High | R01a, R07 | Plan 179 steps 2–9, one Core integration PR |
| R04 | Continued authority / authority | L | High | R02, R03 | Core membership/renewal after two expiry cycles; bounded self-issue |
| R05 | Diagnostic redaction / hardening | S | Medium | None | Plan 170 negative inspect/crash cases |
| R06 | Verified restores / hardening | M | High | None | Plan 171 consumer policies; staged fail-closed recovery input |
| R07 | Canonical strictness / codec | S–M | High | None | Plan 172 valid-byte parity plus duplicate/noncanonical refusals |
| R08 | Deadlines and pagination / carrier | M–L | High | None | Plan 173 bounded setup, interrupted large-log real-transport replay |
| R09 | Input limits / hardening | M | High | R07, R08 | Plan 176 ingress and authority boundary refusals |
| R10 | Root-only Treehouse domain / product + parity | XL | High | R01a, R07, R09 | Core complete vocabulary, effects, conflicts, offline demo |
| R11a | Local catalog/provisioning / carrier | M | High | R02, R04, R06, R10 | Local signed catalogs, root bootstrap and bounded routes |
| R11b | Lifecycle reconciliation / carrier | M | High | R11a | Crash-safe creation/removal, no phantom publication |
| R11c | Catalog replacement trust / recovery | M | High | R11b | Retained client accepts authorized replacement without old signer |
| R12 | Empty local app / product | L | Medium | R01a, R05, R06, R10 | Packaged offline preview; no member grants |
| R13 | Enrollment and sync / product | L | High | R01b, R08, R11c, R12 | Packaged two-app join/heal; wrong-input and four-socket tests |
| R14 | Profiles, roles, removal, renewal / product + authority | XL | High | R13, R04, R36 | Packaged enroll-then-pin, per-replica readiness and renewal |
| R15 | Measured rollover / product | L | Medium | R14 | Packaged archival saga, finite-capacity stop, retained readability |
| R16 | Member reseed, AF-1 / recovery | M | High | R06, R11c, R15 | Packaged new-identity relay reseed from surviving member evidence |
| R17a | Native witness decision / native custody | M | High | None | Plan 174 decision plus concrete Android build plan |
| R17b | Native witness signing / native custody | L | High | R17a, R01b, R03, R14 | Claim verification, protected signing, consent/replay negatives |
| R17c | Independent witness proof / device QA | M | High | R17b | Physical custody/presence evidence for the selected Android profile |
| R18 | Founder and combined loss / recovery | M | High | R04, R11c, R15, R16, R17c | Packaged loss with physical witnesses; independent retained trust |
| R19a | AF-3 contract / identity | M | High | R01a, R02 | Adopted vouch, conflict, possession and lost-loan rules |
| R19b | AF-3 semantics / identity + parity | L | High | R19a, R04, R10 | Core founder-present/absent re-admission with negative controls |
| R20 | New-phone re-admission / product | M | High | R14, R19b | Packaged AF-3, old-key return, visible unresolved grants |
| R21a | WSS/restore artifacts / operations | M | Medium | R05, R06, R08, R09, R11c | Reproducible deployment/runbook and local restore checks |
| R21b | WSS field restore / operations | M | High | R21a | Public WSS, restart and clean-host backup restore; operator inputs |
| R22a | Signed Android artifact / release | M | High | R15, R16, R18, R20, R21a | Immutable internally distributed candidate and manifest |
| R22b | Physical candidate / device QA | M | High | R22a, R21b | Exact candidate passes all group, loss, network and upgrade gates |
| R23 | Community pilot / pilot lead | 14+ days | High | R22b | Participant evidence, incident disposition, continue/fix/stop |
| R24 | Exact custody v2 / custody + parity | L | High | R03, R09, R11a | Core exact grant/request/loan/due binding; legacy refusal |
| R25 | Admission, decline, disputes / custody + parity | M–L | High | R24, R19a | Core adopted versioned facts, precedence and lost-signer limits |
| R26 | Toolshed module / product | L | Medium | R01c, R11c, R14, R23, R25 | Packaged inventory and Tool-root day ceremony; isolation/copy tests |
| R27 | Offline QR co-signing / product + device QA | L | High | R13, R25, R26 | Physical borrow/return and independent authorized third-device audit |
| R28 | Factual ledger / projections | M | Medium | R25, R26 | Core/UI across authorized Tool logs, zero writes |
| R29 | Subject-present receipts / product | M | High | R20, R27, R28 | Packaged fresh same-key challenge and selected-evidence disclosure |
| R30 | Household pilot / pilot lead | 7–14 days | High | R23, R27, R28, R29 | 5–8-household pilot with explicit decline, pending and dispute cases |
| R31a | Optional desktop host / carrier | M | Medium | R08, R11b, R16, R23 | Scoped sidecar runtime; retained identity and bounded resources |
| R31b | Packaged host proof / release | M | High | R31a | Supported packaged restart/reseed; scope approval if needed |
| R32a | Optional iOS adoption/archive / release | M | High | R23 | Separate scope/hardware/signing approval and archive |
| R32b | Physical iOS / device QA | M | High | R32a | Exact iPhone candidate loss/reboot/upgrade proof; R30 if Toolshed included |
| R33 | Measured expansion / performance | Program | High | R23, R35b | Supported count AND lifetime/device envelope; staged field evidence |
| R34 | Township follow-on / product | Program | Medium | R23 | Re-baselined B1/B2 plan; R30 additionally gates its product pilot |
| R35a | Lifetime/target decision / product + performance | M | High | R15, R23 | Observed growth, adopted boundary/retention decision or stop verdict |
| R35b | Lifetime policy proof / lifecycle | L or no-build | High | R35a | Implemented/tested adopted policy or tested finite end-of-life |
| R36 | Protected witness identity / native custody | L | High | R01b, R12, R17a | Distinct persistent witness keys and reviewed public-identity binding |

### Work order and merge discipline

Initial ready work: R01a, R05, R06, R07, R08 and R17a. Prioritize R02 and R17a early because they can invalidate the release design; prioritize R07's small codec correction before R03. R07 and R08 have no dependency on each other. R09 requires both. Run independent ready work sequentially unless multiple executors are explicitly authorized; with a team, use up to three implementation lanes and one integrator.

Two paths meet at the working group: R07/R08 → R09 → R10 → R12 produces the preview; R02/R03 → R04 → R11a/b/c supplies continued authority and provisioning. R17a → R01b/R36 supplies the protected identities. They converge at R13/R14; then R15, loss and operations work feed R22 and R23. This is a dependency explanation, not an extra edge list.

Reserve one writer at a time for `authority.ex`, `authority.ts`, `codec.ts`, `carrier.ts`, the exporter/vector corpus, native shared state, lockfiles, generated mobile projects and workflow YAML. R03 and R10 may prepare independently; shared-file integration is serialized, preferably R03 first when ready. That ownership preference is not a false semantic dependency that blocks the offline preview if R03 stalls.

Atomic merge units: R03, R04, R10, R14, R19b, R24 and R25 each keep BEAM, TypeScript, bytes, projections and affected claim contracts together. R08 pagination keeps server and both clients together. Plan 179's preparation stages are RED authoring/oracle → BEAM judge → exporter → TS/leased authoring → compaction/dump → full gate/claims, one implementation PR. R10 preparation is membership/invites → posts/effects → roles/archive → reciprocal integration, one XL merge. R14 preparation is profile pinning → removal/fan-out → renewal UX, one integration unit.

Every packet closes with exact base/PR/merge SHAs, changed files, named tests and results, review findings and disposition, required PR-tip and merge-result checks, achieved evidence tier and remaining limitations. Each implementation packet receives an adversarial review of its exact diff; semantic fixes receive another review. A field packet closes with an immutable evidence record. Code-complete waiting for hardware is a separate status, never DONE. Shared CI failure, missing approval or budget blockage is recorded as a blocker, not a passing check.

## 4. Product behavior that must survive the synthesis

### Domain and archival: R10, R15

Use the existing Replica DSL and Plan 178 vocabulary plus the adopted `archive thread` extension. A command retains one signed op ID, one DAG node and one all-or-none verdict, with ordered `effects[]`; singular effects normalize to a one-element array. An invalid effect invalidates the whole command. Reciprocal BEAM/TS authoring, byte parity, projection and quarantine must agree.

Archive belongs to `Treehouse.Thread`, in an `archived` field with `authority: :moderator`. Command policy sees only that Thread op's causal ancestors and verdicts; a Space flag cannot authorize Thread-local denial. An admin needs the moderator role to archive. No unarchive enters the first pilot.

Application refusal precedence is: missing/not-causal target; quarantined target; wrong target kind/Thread; wrong author; already tombstoned; archived Thread. An honored archive in the causal past denies new posts, author edits and author tombstones; moderator tombstones remain allowed. A post concurrent with archive remains honored. Existing canonical ordering resolves concurrent edits; tombstoned content cannot be revived. R10 proves create, concurrent admission/posting, partition/heal, tombstones, role transfer, removal, archive and dump/restore across both runtimes. Root-only succession replay is qualified until R04 establishes its bounded policy; legacy Township and Toolshed vectors remain green.

R15 makes rollover a durable saga with one locally identified attempt, retryable successor draft/provisioning and explicit recovery after each transition. Check slot capacity before asking the user to archive for rollover; archive may still be a deliberate standalone action. If the operator or network disappears after archive, show the archived Thread and pending successor; retries cannot duplicate it. A live Space reference is published only after route readiness. The successor receives the approved per-replica profile and current reviewed roster; it is not automatically a copy of stale invitations or removed members. Copying a refused offline draft to a new Thread is an intentional new signed action.

### Membership and transport: R11, R13, R14

The signed semantic Space reference and signed transport catalog are separate. An extra route grants nothing; a missing route is unavailable, not an empty Thread; wrong-replica/service/catalog metadata refuses. `local_draft → genesis_created → carrier_pending → listed` survives a crash at every step. Only the named operator invokes `pilotctl` on the host; this program adds no public administration API.

R11a uses explicitly disposable signing identities for local proof. Before real provisioning, the operator generates the catalog signer, the living root reviews/signs its product/replica/service bootstrap binding, and encrypted separate signer backup is verified with named custody/rotation responsibility. Packaged loss drills use independently created test-profile credentials through that same ceremony; the candidate/field gate uses its selected pilot profile. R11c also tests replacement when the old service and catalog signing keys are unavailable, so possession of a backup cannot conceal an unimplemented member-authorized trust path.

A joiner generates its own member/carrier identity. Review recipient, group, service and per-Thread scope before issuing exact-audience grants and transport admission. QR/deep links contain no private signing material or bearer invitation secret. Keep review/use, signing/posting and Sync distinct. Permission denial/regrant, cold-start links, stale/rebound/revoked invitations and wrong product/recipient/replica/server inputs cause no unauthorized durable changes.

When a catalog changes after an invitation was signed, obtain freshly reviewed grants for the new set. New members read archived Threads only when explicitly listed and granted. Four foreground sockets fairly serve current and archived Threads, with visible/current priority and a full-history sync action; background delivery is not promised.

Member removal fans out across Space, every current/archived Thread and transport admissions, using each actual issuer. Show unresolved semantic grants and `removal_pending` until reconciled. A Space admin cannot revoke another issuer's grant by UI assertion. Offline/concurrent authority behavior follows causal rules; historical plaintext cannot be retracted. Tool-root authority later remains distinct as well.

### Native preview: R12

Fresh boot is empty, with no Township fixture, seeded peer or implicit identity import. A preview contains a local root profile, Thread, drafts and outbox, but no member grants. Key/history/drafts survive restart and migrations fail closed. Unknown product/schema refuses. The preview visibly remains local and `recovery_not_ready`; signing locally does not mean relay acceptance.

## 5. Make recovery a lifecycle, not one successful beacon

### Authority contract: R02–R04

R02 freezes a matrix for Space, existing Threads, a newly created Thread and future module Tool roots. For every action name its authorized signer and required retained evidence: admit/remove, issue/renew/revoke grants, acquire admin/moderator, advance epochs, create child roots, admit transport peers, and replace endpoint/catalog identities. Founder and relay operator cannot be hidden semantic signers. Explicitly test founder loss before pinning, partial pinning, expired ancestor grants and loss of a witness.

R03 executes Plan 179 without changing legacy dormancy or pre-existing vector bytes. Include ancestry-resolved beacon policy, quorum/author/dependency binding, step bound `1..65_535`, horizon `2^53-1`, replay/partition negatives, public expiry parity, TS leased-delegation authoring, compaction mirror and fresh-VM dump vocabulary. Its claim is narrowly Sim/TS witnessed epoch advance, pre-loss delegated admission, surviving-issuer revocation and leased-grant lapse. It does not close general founder loss or a device ceremony.

R04 owns the separate, explicitly versioned/approved successor-scope repair proposed in Plan 180's 2.1, including deliberate succession-vector changes outside Plan 179's byte-preserving PR. Test the predecessor's effective operation scope as a candidate bound; specify causal scope, attenuation and parent-expiry behavior before selecting a policy. Preserve old-log interpretation unless a reviewed contract expressly changes it.

R04 must remove the founder, admit/remove members, renew authority across at least two expiry cycles and authorize a new Thread in both runtimes. An expired delegation chain whose only issuer is the dead founder is not renewal. An unleased founder grant is a negative control and residual limitation unless an adopted new policy replaces it. Root identity stays immutable. Below quorum, hostile quorum, and unavailable witness rotation have explicit outcomes. If bounded continuation cannot be proved, stop live provisioning and the strong pilot; R10/R12 remain the offline preview.

### Enrollment, custody and pinning: R36, R14, R17

R17a makes the native architecture decision early. R36 creates distinct protected governance-witness keys and exports only public identity/metadata through typed native operations. Member/carrier and witness keys retain separate purposes; generic signing, secret export, cross-product aliasing and silent software fallback refuse. If identity binding needs possession proof, specify a domain-separated fixed challenge that cannot sign a governance claim. Independently keyed devices, persistence and wrong-caller/member/recipient/session/replay cases are mandatory.

The living root first enrolls members, reviews their member-to-witness public-key bindings, then authors a valid later genesis to pin the approved beacon/succession/recovery profile for every required replica. Both genesis records remain in history. Pinning all supported replicas, auditing grants and confirming protected custody precede readiness; actual physical ceremony proof still belongs to R17c. Existing unleased grants must be revoked/reissued while their issuer lives or readiness refuses. A lost root cannot retrofit missing pins; AF-3 cannot rotate a genesis root or a witness set. A new Thread has its own authorized creation/pinning path.

R17b shows the group, precise action/epoch, dependencies and lease consequences to each witness. Native verification, caller/session binding, cancellation, substituted claims, duplicate signers, interrupted collection and changed-dependency consent reuse are tested. R17c supplies independent physical Android signing/presence evidence. macOS `LAContext` proof is not Android proof; Plan 146 codesigning is a dependency only if that platform is chosen. Unproved custody blocks R36/profile readiness; unproved claim verification or presence blocks R17c and the pilot. There is no automatic downgrade to software-only witnesses.

### Signed time and renewal: R02, R14, R18, R23

Leases lapse through valid signed epochs, not wall time. Without an advancing beacon they do not lapse. R02 adopts exact units, inclusive expiry arithmetic, warning window, permitted catch-up, issuer and maximum jump, using Plan 179's bounds. Seven signed-day leases with a two-day warning and a daily reviewed epoch are candidates, not already accepted constants. Group epochs and Tool-root-signed UTC-day assertions are distinct; witnesses do not inherit Tool clock authority.

Measure before adopting the cadence: 12 members × 13 group replicas is up to 156 grant renewals per cycle, 312 across two cycles before additional role grants. Count actual signatures, prompts, bytes and operations. R14 provides bounded, reviewed exact-recipient/replica renewal batches with per-grant results and idempotent retry, or R02 chooses an operable longer window. A batch grants no arbitrary signing power. Stale epoch, issued/expiring/lapsed grants and pending/failed renewal are visible. Name a coordinator and backup, rehearse missed quorum/operator availability, and preserve evidence without hidden clock or unbounded-grant fallback.

### Three losses and their composition: R16, R18–R20

| Scenario | Surviving inputs | Required positive and refusal evidence |
| --- | --- | --- |
| AF-1 relay destroyed | Member-retained verified frames plus public pairing/admission/catalog trust | New service identity/path, no old disk or server backup, installed-client trust replacement and convergence; corrupt/wrong-root/oversized/partial bundles refuse before replacement |
| AF-2 founder lost | Pre-pinned quorum, surviving bounded issuers and per-replica policies | Role continuation, membership and renewal over two expiry cycles, new Thread/rollover; no founder signature, hidden root or test-presence bypass |
| AF-3 member phone lost | Fresh new key plus adopted current-member attestations | New-key possession, explicit admission, auditable old/new link and eventual old-key exclusion under actual revocation/lease rules; founder-present and founder-absent cases |
| Founder + relay/catalog signer lost | Retained member evidence and pre-authorized replacement trust | AF-2 plus new endpoint/catalog authorization and operator-assisted provisioning; neither dead signer nor old host backup supplies authority |
| Same-identity disaster restore | Encrypted operator backup and separately held recovery credentials | Signed cutoff restored on a clean host; client-retained later ops replay; this is R21, distinct from AF-1 |

R06 verifies untrusted recovery input before any reseed or audit consumer trusts it. Stage and validate imports before state replacement; corrupted bytes never silently become a new identity or empty group. R11c proves replacement against an installed client's retained trust, including forged/rollback/cross-product replacement negatives; a fresh client trusting an injected key is insufficient.

R19a freezes the group-scoped vouch artifact, current-member threshold and deterministic competing-link policy through existing op kinds. A new command requires its exact Plan 178 vocabulary amendment before R19b. Bind old/new key, Space, possession challenge and evidence context. Subthreshold, removed/stale witnesses, outsider/never-member old keys, cross-Space replay, substituted new keys and competing links get explicit outcomes. The attestation itself confers no capability. Historical signatures and old-key receipt attribution remain unchanged.

R20 shows new admission separately from old-key revocation/lapse and transport removal. Reintroduce the old phone as an adversarial case. Group attestation does not recover a private key, let a new key sign old receipts, or close an old-key loan. These are ordinary member continuity facts, not universal identity, key rotation or automatic recovery.

A retained copy proves only its retained evidence. Compare known frontiers and concrete missing ops; an isolated stale copy cannot prove completeness. Plan 167 is required before claiming an automated divergence explanation; a core Township helper alone does not establish a Treehouse product path. Until a tested adapter exists, present frontiers and unknown completeness. No plan can recover operations retained nowhere.

## 6. Finite capacity and release gates

### R15 volume policy and R35 lifecycle

The proposed warning is 3,200 operations or 6.4 MiB. Pilot stop: 4,000 operations, 8 MiB or a cold open over five seconds. Count stored evidence, including authority and quarantined operations, not just visible messages. Stop local new posting at the threshold while retaining bounded ingestion needed to heal; offline replicas can overshoot, so this is not a coordinated global hard cap.

Archives retain history, routes, admission records and slots. Twelve total Threads × 4,000 operations is roughly 48,000 Thread-log operations over a Space's initial lifetime, excluding the separate Space log; byte/latency limits and authority overhead reduce usable messages. At 150 members that is only about 320 Thread operations per person. Keep one group per supported host: one Space + 12 Threads + one Shed + 20 Tools = 34 routes, within the 64-route bound; phones retain four foreground sockets. No silent second full group or archive-slot reclamation is supported.

R35a uses observed pilot growth to adopt a finite-lifetime policy or a specifically designed archive/route change. At exhaustion, preserve readable/exportable history and refuse new posting/Threads. Starting a new Space is explicit new genesis/admissions, not automatic migration or identity continuity. R35b either builds and tests the adopted change or proves the no-build end-of-life behavior. Any revised policy must pass removed-member, cold-device full-history retrieval, storage, recovery and parity gates while retaining authenticated history. Production compaction remains a separate program.

Only then may R33 test proposed 12/50/150 workloads and an adopted 149/150/151 admission boundary. Declare both supported member count and measured lifetime/storage envelope on minimum supported phones. If lifecycle is unacceptable, expansion stops at the observed 9–15-person cohort. Optional iOS and host work cannot quietly widen this envelope.

### R21 operations

Use the existing pilot release behind a TLS edge on a supported Linux host, loopback listeners, mounted secrets, health/readiness, durable volumes and redacted diagnostics. Name host/DNS/TCP 443, certificate renewal, encrypted off-host daily backup, signed cutoff manifest, separately held recovery key, clean restore host and on-call operator. Verify acknowledged-op durability through process/host restart, corrupt-restore readiness refusal, Wi-Fi/cellular WSS, certificate renewal and backup-age alert. The operator accepts a 24-hour disaster RPO; later data requires a surviving client copy.

Local artifacts can be built before access exists. Missing hosting, backup or hardware inputs leave field gates open. Use the pilot carrier boundary, not an implicitly promoted legacy `lattice_server` endpoint. Content-bearing exports remain member controlled and out of central telemetry.

### R22 exact physical candidate

Record immutable Git SHA, APK hash, non-debug pilot signing fingerprint, version, device model/API and installed-artifact match; keep full device serials local. Use unrelated physical phones and enough independent witness devices for the adopted quorum after founder loss. No seeded identity, fixture fallback, developer tether or public cleartext route supplies the proof.

Required matrix: real QR/deep-link join with denial/regrant/cold-start; wrong recipient/product/replica/service/tampered/rebound/revoked refusal; offline post/edit/heal and tombstone convergence; manual admin/moderator transfer and stale-holder refusal; revocation and pending removal; force-stop/reboot; signed N→N+1 upgrade retaining keys/history; carrier restart and both recovery procedures; AF-1/AF-2/AF-3 and combined loss; independent audit matching Sim and a separate client. R22 owns release evidence even when it reuses earlier packaged tests.

Benchmark separately at 5,000 operations/10 MiB for three cold opens each meeting five seconds on supported pilot devices. This deliberately exceeds the product stop to prove headroom. Use a named synthetic profile and approved scoped reset/deletion, never user data. Simulator, source, native-build and physical evidence remain separate.

### R23 pilot acceptance

Recruit 9–15 people for at least 14 days. Give them a plaintext-reader disclosure, finite-lifetime limits, assisted provisioning expectations, signed release, ordinary workflow instructions, recovery/export path and a named support owner. Record loss drills on agreed synthetic profiles and everyday use on the community profile; a drill is not proof that real data was lost and recovered during ordinary use.

Before enrollment, the pilot lead records minimum-supported devices, expected daily use, incident/support response targets and consented evidence collection. Measure completion/abandonment of create/join/post/edit/rollover, reconnect/outbox failures, cold-open latency, growth, provisioning waits and renewal workload without message-content telemetry. Every participant must complete onboarding and an offline-post/heal exercise; sample other workflows explicitly in the report rather than treating downloads as adoption. Record daily epoch ceremonies and two renewal cycles, or extend observation until the adopted policy is exercised. Controlled signed-day acceleration belongs to test profiles, not a hidden change to real pilot leases.

Hard stop for unexplained lost acknowledged/retained operations, unauthorized visible effects, identity reset, irrecoverable outbox, failed required loss recovery or capacity-stop failure. Pause the affected workflow for consistency/authorization defects, preserve evidence, fix/review/retest the exact candidate, and restart its affected observation window. Other friction is recorded against the predeclared usability/support targets. Completion requires an explicit continue/fix/stop report, not fourteen elapsed days or a screenshot.

## 7. Toolshed follows the proven group

R24/R25 semantic preparation may proceed under their dependencies; module enablement waits for R23 and R01c. One member key may sign group and module receipts within this group; governance-witness keys remain separate. Each Tool retains its own root, issuer, route and grant scope. Space membership/admin authority does not silently confer custody or Tool-root powers.

| Packet | Behavior and acceptance |
| --- | --- |
| R24 | Implement `custody_offer_v2`, `custody_request_v2`, `custody_transfer_v2` in both runtimes. Bind exact parties, direction, Tool, request, original grant, active loan and due epoch. Borrow needs valid authority and receiving consent; return binds the original borrow and remains possible after grant expiry/revocation. Wrong party/loan/grant, reused request and unilateral return refuse; concurrent valid resolutions select one canonical winner. Preserve v1 bytes and quarantine from beta custody as `:legacy_custody_v1`, without restore crashes. |
| R25 | Invitee-bound admission, `custody_decline_v2`, completed > declined > pending precedence, and an adopted durable dispute command/payload through existing op kinds. Amend Plans 158/0007 and relevant tests before new semantics; change Plan 178 only if its group vocabulary changes. Record each party's signed assertion separately from co-signed facts. A lost-key claimed physical return may be a dispute fact; ordinary old-key loan closure stays unavailable unless a separate exceptional policy is adopted and proven. |
| R26 | Enable one Shed/20 Tools in Treehouse namespaces; update TS/Rust product-manifest contracts, keep legacy Toolshed IDs dormant and Township isolated. Include root-only reviewed `Assert current day`, exact provenance, wrong signer/rollback/duplicate refusal, large-jump review, and stale state after root loss. Group witnesses do not advance Tool time. Correct Toolshed hosting/module copy with a focused contract test. |
| R27 | Reuse Plan 160's actual QR-physics evidence. Two phones borrow/return offline with exact review and durable completion on both; cancelled/partial/repeated/replayed exchanges recover or refuse explicitly. Record decode, consent and durable-completion intervals. A third phone with a fresh, explicitly admitted/read-granted identity independently reconstructs the same audit; outsiders have no implicit access. |
| R28 | Pure BEAM/TS per-member facts across authorized Tool logs: transfers, due-bound returns, open requests with signed-epoch age, disputes and coverage. Zero writes on observation/export. Missing/stale epochs show unknown/stale; no score, total, rank, rate or wall-clock punctuality assertion. Pending is not refusal or wrongdoing. |
| R29 | Subject chooses receipts and signs a fresh challenge bound to shed, verifier/session, selected-evidence digest and nonce with the receipt-signing key. Validate both custody signatures and log lineage; wrong subject/key/shed, replay and selection substitution refuse. Display selected receipts and known disputes with coverage limits, never a completeness claim. A valid AF-3 link cannot prove possession of a lost receipt key. |
| R30 | Run 5–8 households for 7–14 days: ordinary/late return, explicit decline, unanswered request, dispute and loss-key/open-loan rehearsals. Name each Tool's day-assertion owner and record stale-root behavior. Participants complete flows without operator signing; report friction, failures and continue/fix/stop. This does not satisfy a later 90-day/two-neighborhood claim. |

No negative broadcast, cross-shed registry, portable reputation score or fourth Treehouse role is introduced. Old receipts remain readable and verifiable after key loss, while fresh same-key presentation and ordinary old-key custody closure may be impossible. Disclose this before the household pilot.

## 8. Optional work and claims

R31 starts after the community pilot: package the supported desktop sidecar with explicit start/stop, identity, paths, admissions, resource limits, restart and member reseed. Reuse QR image/deep link; additional LAN discovery stays withdrawn. State the host's readers and power to withhold. New parked physical-host scope needs its own adoption. Mobile hosting, NAT traversal and availability guarantees are not implicit.

R32 follows Android with separately approved physical iOS scope, Apple credentials and exact TestFlight/signing/Keychain/camera/link/network/reboot/upgrade/loss evidence. If the module is included, R30 is also a blocker. R34 may re-plan Township after R23; its product pilot waits for R30 and its own domain/device gates. Treehouse proof does not establish Township civic-authority guarantees.

Plans 164/166 are optional local verification improvements; their actual dependency rules still apply, and Plan 166 does not authorize casually removing protected prose tests. Plan 167 enters before an automated product divergence claim. Plans 146/174 enter through the chosen platform's witness contract. Preserve the existing M3/M4/M6 boundaries, frozen false attestation stub, and exclusions for key reconstruction/rotation, E2EE, production compaction, federation, cross-community identity, notifications, media, bots and background-delivery guarantees.

| Evidence completed | Permitted statement and remaining boundary |
| --- | --- |
| R03 | Sim/TS witnessed epoch advance and the narrow admission/revocation/lapse cases; not general founder survival |
| R04 + R19b | The approved profile passes core founder continuation and member re-admission; no packaged, physical or availability claim |
| R10/R12 | Deterministic domain and packaged local preview; no operating community |
| R16/R18/R20 | Named packaged loss workflows pass with stated retained-evidence and witness assumptions |
| R22b | Exact independent physical Android candidate passes the named matrix over disclosed public WSS |
| R23 | One assisted community pilot completed; name observation period, cohort, readers, withholding/provisioning operator and unresolved limitations |
| R30 | One household module pilot completed; factual custody evidence with signed-day and lost-key limits |
| R31b | Supported member-hosted desktop relay works with enumerated readers and withholding host; no centerless/availability claim |
| R33 | Only the measured count, workload, devices and finite lifetime are supported |

Plan 177's broad founder-loss wording stays blocked until both AF-2 and AF-3 pass; claim tier must also match the actual evidence. A green baseline suite or a copy-contract test proves none of these future product milestones.

## 9. Executor reference and verification

Before an implementation session, read [AGENTS.md](../../AGENTS.md), the current [plan index](../README.md), this packet and its mapped source plan in full. Recheck blockers and preserve unrelated work. Use a fresh worktree at the latest accepted immutable base, with a `codex/` branch, explicit file ownership and recovery points before any destructive cleanup. Source-plan scope conflicts become concrete amendment work, not silent implementation choices.

| Packets | Required source / existing seams |
| --- | --- |
| R01/R10–R15 | [158 program map](../158-real-device-beta-poc-program-map.md), [177 re-aim](../177-group-first-antifragile-reaim.md), [178 contract](../178-treehouse-contract-correction.md); Replica DSL, command causal context, shared native product/storage/ingress; Treehouse domain/realm/app files are proposed |
| R02–R04 | [175 spike](../175-succession-tick-provenance-spike.md), [179 build](../179-witnessed-beacons-af2-founder-loss.md), [decision record](../../docs/research/succession_tick_provenance.md), [succession ADR](../../docs/adr/0004-succession-validation.md); `authority.ex`, `sim.ex`, `log.ex`, TS authority/codec and vector exporter |
| R05–R09 | [170 redaction](../170-redact-private-keys-from-inspect-and-crash-reports.md), [171 restore](../171-audit-bundle-verifies-signatures.md), [172 canonical](../172-ts-canonical-encoder-strictness.md), [173 transport](../173-bounded-carrier-transport.md), [176 limits](../176-fail-closed-input-validation.md) |
| R11/R16/R21 | Plan 158 catalog/deployment tickets; carrier runtime/holder/health/manifest, `config/runtime.exs`, release config; [AF-1 tests](../../apps/lattice_carrier_server/test/relay_reseed_test.exs) |
| R17/R36 | [174 native decision](../174-governance-witness-verification-spike.md), Plan 146's selected-platform work; native `ensure_governance_witness_key`, `governance_witness_public_key`, `reject_governance_carrier_alias`; Plan 179 certificates |
| R19/R20 | Plan 177 AF-3 and adopted continuity amendment; signed evidence/delegation/revocation; new continuity artifact and UI |
| R22 | Plan 158 physical gate; existing Android signing/distribution manifest and non-destructive harness; flagship CI |
| R24–R30 | Plan 158 custody/QR tickets, [0007 consent](../0007-co-signed-consent.md), [160 QR physics](../160-pd003b-qr-ceremony-spike.md); Toolshed `shed.ex`, `tool.ex`, `read_model.ex`, consent vectors |
| R31–R35 | [150 host mode](../150-device-hosted-carrier-boundary-cd1.md), Plan 158 later product/scale decisions, Plan 177 boundaries; measured pilot evidence |

Use public-seam positive/refusal, partition/heal, restart/restore and cross-runtime tests proportionate to the packet. Preserve meaningful RED evidence for new behavior. Verify generated source/vector/build consumers; wire new Treehouse/recovery gates into real CI instead of inventing already-existing script names.

Local mandatory toolchain from the executing worktree:

```sh
PATH="$HOME/.asdf/installs/erlang/28.3.1/bin:$HOME/.asdf/installs/elixir/1.19.5-otp-28/bin:$PATH" ~/.asdf/shims/mix verify
PATH="$HOME/.asdf/installs/erlang/28.3.1/bin:$HOME/.asdf/installs/elixir/1.19.5-otp-28/bin:$PATH" ~/.asdf/shims/mix check
```

Run applicable per-boundary Sobelow in `apps/lattice_server` and `apps/township_web`; raw Cowboy carrier changes require authentication, frame-bound, read-only and relay refusal gates instead. Follow mapped plans' actual commands for TS typecheck, canonical/conformance/authoring/build, mobile-core and native product contracts. Browser, stress, packaged and physical gates require the corresponding execution scope; drafting this document does not run the delivery program.

For contract amendments, run the concrete suites `apps/lattice_core/test/township/audit_bundle_test.exs`, `apps/lattice_core/test/township/read_model_test.exs` and `apps/lattice_core/test/treehouse/contract_test.exs`, plus AF-1 and relevant TS/Rust product-manifest gates. Preserve Plan 179's named legacy vector bytes during R03/R07; R04 explicitly owns any approved successor-vector replacement afterward.

Pause dependent work on: unadopted contract/scope change; inability to identify a surviving authorized signer; missing/unproven witness custody; fail-open restore/ingress; secret exposure; BEAM/TS semantic mismatch; an unrelated pinned-test edit; a hidden operator/root dependency in a loss proof; failed release gates or loss of retained signed history. Preserve evidence, report the exact blocked action and continue independent authorized work. An operator approval is collected against the finished proposed amendment or prepared deployment, never invented by the executor.

### Planning-artifact completion

This proposal is complete as a document when its two source comparisons are traceable, every packet has an owner/risk/size/dependency/exit, the graph is acyclic, local links resolve, the diff preserves source artifacts and unrelated contracts, and repository formatting/full-suite verification is recorded. This does not close R01 or any implementation packet. The comparison records validation actually performed for this revision.
