# The directory Mix will write compiled artifacts to.
/_build/

# If you run "mix test --cover", coverage assets end up here.
/cover/

# The directory Mix downloads your dependencies sources to.
/deps/

# JavaScript browser E2E dependencies.
/node_modules/
/clients/lattice-client/node_modules/
/clients/township-tauri-shell/node_modules/
/clients/township-tauri-shell/dist/
/clients/township-tauri-shell/src-tauri/gen/schemas/
/clients/township-tauri-shell/src-tauri/target/
/output/playwright/
/output/flagship/
/output/liveops/
/output/browser_beam_carrier/

# Where third-party dependencies like ExDoc output generated docs.
/doc/
apps/*/doc/

# Temporary files, for example, from tests.
/tmp/

# If the VM crashes, it generates a dump, let's ignore it too.
erl_crash.dump

# Also ignore archive artifacts (built via "mix archive.build").
*.ez

# Local editor/session artifacts.
.cursorindexingignore
.expert/
.specstory/

# Local toolchain override (mise: defer Erlang/Elixir to asdf's OTP 28).
.mise.toml
