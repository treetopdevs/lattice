# lattice_demo

Demo processes and commands for the Lattice POC:

- `Lattice.Demo.EchoServer`.
- `Lattice.Demo.SecretServer`.
- `Lattice.Demo.TabWorker`.
- `mix lattice.demo`.
- `mix lattice.server`.

See `scripts/lattice_poc_demo.sh` for the deterministic demo command.
