# lattice_core

Core Lattice capability plane:

- `Lattice` facade.
- realm, tab, and capability structs.
- cap store, gateway, topology, audit.
- minimal movable process prototype.

See the root `README.md` and `docs/` for the full POC narrative.
