# lattice_server

Browser-facing Lattice server boundary:

- Cowboy HTTP/WebSocket listener.
- strict JSON envelope parser.
- static browser demo serving.
- WebSocket transport implementation for tab realms.

See `examples/browser_demo/` and `scripts/lattice_browser_demo.sh`.
