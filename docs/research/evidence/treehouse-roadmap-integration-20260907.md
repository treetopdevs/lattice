# Treehouse integration checkpoint — 2026-09-07

This is an execution checkpoint, not roadmap completion. The primary checkout
is preserved. The integrator owns plans, ledger, workflows, shared authority,
generated artifacts and merge sequencing. Workers own isolated source slices.
Only one main-branch workflow may be pending.

## Foundations and merge queue

- R11 foundation PR78 merged at `ae65aa76c6e843ed5f721c4b936b194a9970990a`.
- R12 preview PR76 merged at `e0e4384db417c6c7389d3650170e45920e5dc2c2`.
- R36 scaffold PR80 merged at `f3a0b5874b1139da0f02f70c08ec11de00e372cf`.
- Prerequisite PR83 passed independent Sol review, root full checks (917 tests
  plus 27 properties, zero failures, three existing exclusions), client and
  preview checks, and both Phoenix boundary Sobelow checks. Merge
  `36d0bfde6798d5358ee57dc49f87436bfc6b2968` exactly matches tested tree
  `191a8b17704052171d75edc376c004b0451ec8f9`. Main run34126355986 passed.
- Journal PR82 tip `54daf31d21d6a754713f494bfddb3725d5182a74` passed
  independent Sol source/document review and hosted run34127610682. Root full
  checks passed on its source candidate; final documentation changes retained
  identical source. Merge `e13958b8896fb0bb2ea7fe6ff7b930705d38be6a` exactly
  matches candidate tree `8bea141a0a5180f7d8c3139740d1d32cd017fa66`.
  Main run34129782391 passed.
- Provider PR84 passed independent Sol review and hosted tip run34078507203.
  Root `mix check` passed on candidate tree
  `73fb2cf482773b71b4e7dfa887b2c5e891184dc9`. Merge
  `b9671a5982e37511574432f1d43ad61cd5b149c4` matches that exact tree.
  Main run34132702099 is pending; no next main merge precedes its success.

## Atomic continuity train

BEAM judgment, authenticated observation, review/assembly, causal heads and the
deny-only graph are connected with TypeScript equivalents in an isolated train.
Historical vector bytes remain literal. Their six-command grants refuse the
newly decoded command as `operation_not_granted`; preview grants remain unchanged.
The adopted vocabulary addition does not activate catalog replacement.

BEAM source and the TypeScript repairs passed independent Sol review. Reciprocal
proof now covers 24 independently signed histories, 48 cold-process decode/restore
checks and two forged-restore refusals. The bounded mirror proof covers 40 legal
stable cuts and 80 append/reconcile folds. Independent Sol reviewed both proof
packets and the evidence API integration. All 87 historical vector files remain
byte-identical; only new semantic corpora were added.

Root full `mix check` passed at `ac101743`: 1,017 tests plus 27 properties,
zero failures, three existing exclusions, format and Credo passing. Client
conformance/canonical/domain/catalog/continuation/codec/semantic checks and
preview tests/build passed. The preview contract still asserts exactly six public
native commands. Initial full-run failures are retained: historical arity and
grant-refusal expectations were updated, without changing their signed bytes.

Compaction integration uses the reviewed evidence tuple API, preserving the
literal seven-key ordinary analysis result and the same beacon judge. This proves
the bounded research mirror, not production compaction or a compacted public
observer. Hosted tip and exact merge-result gates remain open.

## Native vertical slice

Source through `9d4fcad9629242797df5783168ef258fac2fcd12` adds retained
journal ownership, fixed native review/CryptoObject checks, resumed-activity
refusal and original generation coordination. Independent Sol reviewed these
slices and the post-fence alias repair. Root Android host tests and lint passed:
83 total tests, 82 executed, one existing skip, zero failures/errors.

The generator rechecks the fixed alias after committing its fence. Alias presence
or an observation error refuses before generation. This does not provide atomic
AndroidKeyStore compare/create against a hostile same-UID writer. No new public
command is registered. Private protocol, plugin rejection/ACL, proof signing,
cancellation/restart, public export, independent validator and physical evidence
remain gates. R36 is not complete.

## Explicit remaining claims

The rebuilt R12 replay against retained September6 evidence failed to observe
the archived-thread accessibility state and returned an empty UI snapshot.
Historical Keychain approval evidence does not establish the cause of this new
failure. Rebuilt replay remains open.

The dirty QuickJS experiment is preserved separately, off the critical path.
R11 native installation/CAS and replacement remain unfinished. No enrollment,
authority readiness, protected signing, physical proof, recovery, release
candidate or pilot closure follows from these local checks. Device custody and
elapsed pilot observation require their own actual evidence.
